export type FriendsPageConfig = {
	title?: string;                        
	description?: string;                        
	showCustomContent?: boolean;                          
	showComment?: boolean;                   
	randomizeSort?: boolean;                                   
};
